import React, { FC } from "react";
import UserProfileComponent from "../../src/components/app/UserProfileComponent";

const Profile: FC = (props) => {
  return <UserProfileComponent {...props} />;
};

export default Profile;
